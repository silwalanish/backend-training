{
  "name": "@saphall/sort-cost-center",
  "version": "1.0.2",
  "description": "Generate unique list of cost centers ordered alphabetically from a given array of cost centers.",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cost-center"
  ],
  "author": "saphall",
  "license": "ISC",
  "dependencies": {
    "@saphall/sort-cost-center": "file:"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
